name: Run Tests

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .

      - name: Run unit tests
        run: |
          python -m unittest discover -s tests -v

      - name: Test CLI
        run: |
          agent-eval-planner --version
          agent-eval-planner tests/fixtures/agent_contract.md --plan-only -t "CI"
          agent-eval-planner tests/fixtures/agent_contract.md --tools funnel_analytics,open_service_center_ticket -o ./out
          agent-eval-planner validate ./out/suite.jsonl --known-tools funnel_analytics,open_service_center_ticket
